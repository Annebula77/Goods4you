import styles from './loader.module.css';

const Loader = () => {
  return (
    <section className={styles.loaderContainer}>
      <h1 className={styles.loaderText}>Goods4you</h1>
    </section>
  );
};

export default Loader;
